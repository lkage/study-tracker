# PostgreSQL
POSTGRES_USER=tracker_admin
POSTGRES_PASSWORD=changeme_to_strong_password
POSTGRES_DB=study_tracker

# API
NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://tracker_admin:changeme_to_strong_password@db:5432/study_tracker

# JWT (나중에 인증에 사용)
JWT_SECRET=changeme_to_random_long_string
